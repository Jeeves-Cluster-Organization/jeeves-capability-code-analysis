# ============================================================================
# Gateway Requirements - HTTP API layer (FastAPI + gRPC client)
# ============================================================================
# Minimal dependencies for stateless HTTP gateway
# Size target: ~150-200MB (vs ~2.5GB with current shared requirements)

# Include base dependencies
-r base.txt

# Web framework
fastapi==0.109.0
uvicorn[standard]==0.24.0

# Templates (for UI endpoints)
jinja2>=3.0.0
